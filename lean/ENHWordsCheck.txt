ProvenHashes/UMASHENHWords.lean:64:6: error: Tactic `rewrite` failed: Did not find an occurrence of the pattern
  (BitVec.toNat (data.1 + key.1) * BitVec.toNat (data.2 + key.2) / q + BitVec.toNat tag) % q ^^^ ?y
in the target expression
  BitVec.toNat M ^^^ BitVec.toNat (enh key data tag).2 =
    BitVec.toNat M ^^^ BitVec.toNat (data.1 + key.1) * BitVec.toNat (data.2 + key.2) % q ^^^
      (BitVec.toNat (data.1 + key.1) * BitVec.toNat (data.2 + key.2) / q + BitVec.toNat tag) % q

key data : Chunk
tag M : Word
h :
  (BitVec.toNat (enh key data tag).1, BitVec.toNat (enh key data tag).2).2 =
    (have A := BitVec.toNat (data.1 + key.1);
      have B := BitVec.toNat (data.2 + key.2);
      (A * B % q, (A * B / q + BitVec.toNat tag) % q ^^^ A * B % q)).2
⊢ BitVec.toNat M ^^^ BitVec.toNat (enh key data tag).2 =
    BitVec.toNat M ^^^ BitVec.toNat (data.1 + key.1) * BitVec.toNat (data.2 + key.2) % q ^^^
      (BitVec.toNat (data.1 + key.1) * BitVec.toNat (data.2 + key.2) / q + BitVec.toNat tag) % q
ProvenHashes/UMASHENHWords.lean:63:31: warning: This simp argument is unused:
  h

Hint: Omit it from the simp argument list.
  simp only [BitVec.toNat_xor, h̵,̵ ̵enhHighNat]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
